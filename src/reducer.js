export const initialState = {
	user: null,
	isLoggedIn: false
};

// Selector

const reducer = (state, action) => {
	console.log(action);

	switch(action.type) {
		default:
			return state
	}
};

export default reducer;